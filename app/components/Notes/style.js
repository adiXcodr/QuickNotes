import { StyleSheet } from 'react-native';
import {
    StatusBar
  } from "react-native";

export default StyleSheet.create({

   
      body: {
        flex: 1,
        backgroundColor:"#212121"
      },
});