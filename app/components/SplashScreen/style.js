import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  viewStyles: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: '#f7b621',
  },
  textStyles: {
    color: 'white',
    fontSize: 40,
    marginBottom: 30,
  },
});
